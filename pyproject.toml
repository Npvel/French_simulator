[tool.poetry]
name = "french-simulation"
version = "0.1.0"
description = ""
authors = ["Pavel <npvel@mail.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
